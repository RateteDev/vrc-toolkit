import { AvatarsGrid } from "./me/AvatarsGrid";

// The account owner's avatars. Status editing moved to the header widget, so
// this tab is just the avatar grid.
export function AvatarsView() {
  return <AvatarsGrid />;
}
